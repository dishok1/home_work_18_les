[tool.pytest.ini_options]
pythonpath = [
  ".", "app",
]
asyncio_mode = "auto"
# ЗМІНЮЄМО НА "session" — це змусить усі тести працювати в одному спільному циклі:
asyncio_default_fixture_loop_scope = "session"
